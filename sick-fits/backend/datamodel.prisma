type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @unique
  title: String!
  image: String
  largeImage: String
  price: Int!
  description: String!
  # createAt: DateTime!
  # updateAt: DateTime!
  # user: User!
}

